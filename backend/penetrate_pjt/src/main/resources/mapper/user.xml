<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.ssafy.group5.model.mapper.UserMapper">

	<resultMap type="User" id="UserMap">
		<id column="profile_image_url" property="profileImageUrl"/>
	</resultMap>

	<!-- 로그인 -->
	<select id="login" parameterType="User" resultMap="UserMap">
		select *
		from user
		where id=#{id} and password=#{password}
	</select>
	
	<!-- 회원가입 -->
	<insert id="signUp" parameterType="Map">
		insert into user
		values (#{id}, #{password}, #{name}, #{profileImageUrl})
	</insert>
	
	
	<!-- 회원 정보 수정 -->
	<update id="updateUserInfo" parameterType="User">
		update user 
		set password=#{password}, name=#{name}, profile_image_url=#{profileImageUrl}
		where id=#{id}
	</update>
	

	<!-- 회원 정보 삭제 (회원 탈퇴) -->
	<delete id="deleteUser" parameterType="User">
		delete from user
		where id=#{id} and password=#{password}
	</delete>
	
</mapper>